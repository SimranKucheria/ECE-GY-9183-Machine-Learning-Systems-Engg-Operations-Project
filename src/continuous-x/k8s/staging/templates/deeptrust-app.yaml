# Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deeptrust-app
  namespace: deeptrust-staging
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: deeptrust-app
  template:
    metadata:
      labels:
        app: deeptrust-app
    spec:
      containers:
        - name: deeptrust-app
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          ports:
            - containerPort: 8000
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
---
# Service
apiVersion: v1
kind: Service
metadata:
  name: deeptrust-app
  namespace: deeptrust-staging
spec:
  selector:
    app: deeptrust-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
  externalIPs:
    - {{ .Values.service.externalIP }}
